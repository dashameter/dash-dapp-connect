{
  "name": "dash-dapp-connect",
  "version": "1.0.0",
  "description": "Login with Dash",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "browserify index.js --standalone dash-dapp-connect > dist/dash-dapp-connect.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dashameter",
  "license": "MIT",
  "dependencies": {
    "post-message-stream": "^3.0.0"
  }
}
